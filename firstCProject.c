#include <stdio.h>//tells compiler to include information about the standard output/input library
//Main usually calls other libraries and functions to help in execution
main()  //program begins by executing from Main..so every C program must have a main
{
    printf("Hello, world\n");

    /* \n is for new line
        \t is for tab
        \b is for backspace
        \" for the double quote

        ***try using \c *** in your arguments in printf
    
    */
}